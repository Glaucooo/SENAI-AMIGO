create database banco_de_dados;
use banco_de_dados;

create table Cadastro( id varchar(100), nome varchar (100), email varchar (200),
 senha varchar(300), primary key(id, email, senha))
select*from Cadastro

